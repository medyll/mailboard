[project]
name = "jobmailboard-browser-mail"
version = "0.1.0"
description = "Navigation JEV pour le collecteur navigateur de jobmailboard"
requires-python = ">=3.12"

# Épinglé à un commit testé plutôt qu'à main : le projet est jeune et son
# contrat public peut encore bouger. Remonter la référence est une décision,
# pas un effet de bord d'un run.
dependencies = [
  "jev-ultrafast @ git+https://github.com/browser-use/jev-ultrafast.git@1231850a0bf1a0c0341fe408ef1668dbbfdfac46",
]

[tool.uv]
package = false
